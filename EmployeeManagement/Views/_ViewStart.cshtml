@{
    // _ViewStart.cshtml is the first place the program looks for layout.
    // Can be overriden with _ViewStart.cshtml in subfolder or on individual cshtml file.
    Layout = "_Layout";

    /**************************************************************************************
     * Condition statements as to which layout to use, in this case Admin vs. non-Admin
     * 
     * If (User.IsInRole("Admin"))
     * {
     *      Layout = "_AdminLayout";
     * }
     * else
     * {
     *      Layout = "_NonAdminLayout";
     * }
     **************************************************************************************/

}
